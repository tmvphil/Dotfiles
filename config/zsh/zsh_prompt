precmd() { 
    if [ $? -ne 0 ]; then
        STATUS="─(%B%F{red}$?%b%F{blue})"
    else
        STATUS=""
    fi

    if [ $USER != phil ]

    print -rP "%F{blue}┌─[%B%F{green}%~%b%F{blue}]$STATUS" }

PROMPT="%F{blue}└─╼ %f"




